<%= form_for(@team) do |f| %>
  <% if @team.errors.any? %>
    <div class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@team.errors.count, "error") %> prohibited this team from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @team.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <fieldset>
    <div class="field">
      <%= f.label :short_name %><br>
      <%= f.text_field :short_name %>
    </div>
    <div class="field">
      <%= f.label :long_name %><br>
      <%= f.text_field :long_name %>
    </div>
    <div class="field">
      <%= f.label :is_main %><br>
      <%= f.check_box :is_main %>
    </div>
    <div class="field">
      <%= f.label :in_league %><br>
      <%= f.check_box :in_league %>
    </div>
    <div class="actions">
      <%= f.submit class: "btn btn-success btn-sm" %>
    </div>
  </fieldset>
<% end %>
